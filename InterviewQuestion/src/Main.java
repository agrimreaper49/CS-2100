
public class Main {
	//Assume that we have employee
	//type of car, whether or not they are handicap
	for (ParkingRamp r : list of ramps) {
		if (r.getNumber() == 2) {
			ParkingFloor floorThree = parkingRamp.getFloor(2);
			//So right now we are the current floor and ramp
		}
	}

}
